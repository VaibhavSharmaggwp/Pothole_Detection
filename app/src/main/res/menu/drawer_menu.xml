<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Added header section -->
    <item
        android:id="@+id/header"
        android:title="Navigation Menu"
        android:enabled="false"
        app:actionLayout="@layout/menu_header_item">
        <menu>
            <!-- Menu items with improved styling -->
            <item
                android:id="@+id/nav_dashboard"
                android:icon="@drawable/ic_dashboard"
                android:title="Dashboard"
                app:actionLayout="@layout/custom_menu_item"
                android:checked="true"/>

            <item
                android:id="@+id/nav_officer"
                android:icon="@drawable/ic_officer"
                android:title="Officer"
                app:actionLayout="@layout/custom_menu_item"/>

            <item
                android:id="@+id/nav_sdo"
                android:icon="@drawable/ic_sdo"
                android:title="Sub-divisional Officer"
                app:actionLayout="@layout/custom_menu_item"/>

            <item
                android:id="@+id/nav_worker"
                android:icon="@drawable/ic_worker"
                android:title="Worker"
                app:actionLayout="@layout/custom_menu_item"/>

            <item
                android:id="@+id/nav_reports"
                android:icon="@drawable/ic_reports"
                android:title="Reports"
                app:actionLayout="@layout/custom_menu_item"/>

            <!-- Divider before location -->
            <item
                android:id="@+id/divider"
                android:title=""
                android:enabled="false"
                app:actionLayout="@layout/menu_divider"/>

            <item
                android:id="@+id/nav_location"
                android:icon="@drawable/ic_location"
                android:title="Location"
                app:actionLayout="@layout/custom_menu_item"/>
        </menu>
    </item>
</menu>